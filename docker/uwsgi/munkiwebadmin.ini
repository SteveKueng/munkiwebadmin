[uwsgi]
project_name = munkiwebadmin
base = /munkiwebadmin

chdir = %(base)
module = %(project_name).wsgi:application

master = true
processes = 4
lazy-apps = true

socket = /munkiwebadmin/munkiwebadmin.sock
chmod-socket = 664
vacuum = true

pidfile = /var/run/%n.pid
logto = /var/log/uwsgi/%n.log
